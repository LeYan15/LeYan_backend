class Settings:

    URL_CATEGORIES = "products"
    URL_SALES = "sales"
    URL_STORES = "shops"
    URL_FORECAST = "forecast"

    FORMAT = "%(name)s %(asctime)s %(levelname)s %(message)s"
    DATA = "%Y-%m-%d"


settings = Settings()
